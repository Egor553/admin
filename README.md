
# Telegram Mini App: Запись на сессии

Профессиональное решение для записи клиентов на оффлайн и онлайн сессии через Telegram Mini App с интеграцией в Google Таблицы и Salebot.

## 🚀 Основные возможности
- **Выбор города**: Автоматическая проверка доступности оффлайн-сессий в конкретном городе.
- **Интерактивный календарь**: Удобный выбор даты и времени (слотов).
- **Синхронизация с Google Sheets**: Все данные о слотах и записях хранятся в облаке.
- **Интеграция с Salebot**: Автоматическое уведомление бота и продолжение диалога после записи.
- **Панель администратора**: Встроенный функционал для генерации новых слотов (вход через секретный код `admin123` в поле поиска города).

## 🛠 Технологический стек
- **Frontend**: React, TypeScript, Tailwind CSS.
- **Backend**: Google Apps Script (JavaScript V8).
- **Database**: Google Sheets.
- **Platform**: Telegram WebApp API.

## 📦 Установка и настройка

### 1. Google Sheets (Бэкенд)
1. Создайте новую Google Таблицу.
2. Перейдите в `Extensions` -> `Apps Script`.
3. Скопируйте содержимое файла `GoogleAppsScript.js` в редактор.
4. Вставьте ваш Callback URL от Salebot в переменную `SALEBOT_CALLBACK_URL`.
5. Нажмите `Deploy` -> `New Deployment` (тип: Web App, доступ: Anyone).
6. Скопируйте полученную ссылку (URL) и вставьте её в `services/api.ts` в переменную `SCRIPT_URL`.

### 2. Telegram Bot
1. Создайте бота через @BotFather.
2. Создайте новое Mini App через @BotFather, указав URL, где будет размещен ваш фронтенд.

### 3. Salebot
1. В конструкторе Salebot создайте блок с условием `mini_app_booking`.
2. В этом блоке вы можете использовать переменные `#{name}`, `#{phone}`, `#{city}`, `#{booking_date}`.

## 🔑 Доступ администратора
Чтобы попасть в админ-панель для создания слотов, введите `admin123` в поле поиска города на главном экране.
